  <p id="text">
        Pick Your Next Trip
    </p>

<% trip = Trip.read %>

<ul>
<% trip.each do |run| %>

<h2> <%= link_to run["Name"], "/trip/#{run["id"]}" %> </h2>
<%=link_to image_tag(run["Picture_url"]), "/trip/#{run["id"]}" %>

<hr>
<% end %>
</ul>

